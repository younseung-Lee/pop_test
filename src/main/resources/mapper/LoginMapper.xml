<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.pop.mapper.LoginMapper">
    
    <!-- 아이디와 비밀번호로 사용자 조회 -->
    <select id="findByIdAndPw" resultType="com.example.pop.vo.MartIpVO">
        select id,
               ip,
               now,
               edate,
               nm,
               gr,
               pw,
               insert_dt,
               update_dt,
               sho_biz_tel,
               sho_use_yn,
               limited_order_prx,
               mart_opening_hours,
               mart_dvry_area,
               mart_dvry_prx,
               mart_address,
               mart_bank_account,
               mart_bank_account_owner,
               mart_bank_nm,
               mart_order_time,
               mart_order_policyUp,
               mart_order_policyDown,
               pg_mid,
               pg_api_key,
               soldout_count,
               shopping_img_used,
               mart_parking,
               att_yn,
               use_mem_cmrc_yn,
               use_appdc_yn,
               use_pg_yn,
               sep_pwd,
               acct_trsfr_yn,
               mart_main_catgy_title,
               app_order_yn
        FROM pop.mart_ip
        WHERE id = #{id} 
          AND pw = #{pw}
        LIMIT 1
    </select>
    
    <!-- 아이디로 사용자 조회 -->
    <select id="findById" resultType="com.example.pop.vo.MartIpVO">
        SELECT id, pw
        FROM pop.mart_ip
        WHERE id = #{id}
        LIMIT 1
    </select>
    

    
</mapper>
