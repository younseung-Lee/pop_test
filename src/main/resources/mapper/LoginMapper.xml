<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.pop.mapper.LoginMapper">
    
    <!-- 아이디와 비밀번호로 사용자 조회 -->
    <select id="findByIdAndPw" parameterType="map" resultType="map">
        SELECT user_seq,
               mart_cd,
               mart_nm,
               unit_mart,
               als_mart_nm,
               user_id,
               user_nm,
               user_pwd,
               used_yn,
               user_auth,
               reg_dt,
               mody_dt,
               mart_prefix_group,
               push_token,
               device_uuid,
               order_letter,
               order_sort,
               user_password,
               als_mart_nm2
        FROM am_mart.users
        WHERE user_id = #{id}
          AND (user_pwd = #{pw})
          AND used_yn = 'Y'
        LIMIT 1
    </select>
    

    <!-- 아이디로 마트 IP 조회 (기존 로직 유지) -->
    <select id="findById" resultType="com.example.pop.vo.MartIpVO">
        SELECT id, pw
        FROM am_mart.mart_ip
        WHERE id = #{id}
            LIMIT 1
    </select>
    

    
</mapper>
